---
name: feishu-intro
name_zh: 飞书个人介绍
description: Feishu Bot 实时个人介绍功能。自动生成、定时更新、一键分享个人介绍卡片。
metadata: {"clawdbot":{"emoji":"🪪"}}
triggers:
  - 个人介绍
  - 自我介绍
  - 我的介绍
  - 发介绍
  - 更新介绍
  - 定时介绍
priority: 85
---

# Feishu 个人介绍技能

自动生成精美的个人介绍卡片，支持定时更新和一键分享。

---

## 🤖 AI 决策指南

### 用户意图识别

| 用户说法 | 意图 | 执行动作 |
|----------|------|----------|
| "发我的介绍" | 发送个人介绍 | 读取最新介绍并发送 |
| "更新介绍" | 重新生成介绍 | 执行生成脚本 |
| "定时发送介绍" | 设置定时任务 | `openclaw cron add` |
| "每天发一次介绍" | 创建周期任务 | `openclaw cron add --cron` |
| "取消定时介绍" | 删除定时任务 | `openclaw cron remove` |

### 必须追问的情况

- **定时任务没有时间**："定时发介绍" → 询问"请问什么时候发送？"

### 无需追问可直接执行

| 用户说法 | 理解为 |
|----------|--------|
| "发介绍" | 立即发送最新个人介绍 |
| "更新介绍" | 重新生成并发送 |
| "每天早上9点发介绍" | `--cron "0 9 * * *"` |

---

## 📋 命令速查

### 生成个人介绍

```bash
cd /root/.openclaw/workspace-resume-manager && ./scripts/generate-intro.sh
```

### 读取最新介绍

```bash
cat /root/.openclaw/workspace-resume-manager/output/latest_intro.txt
```

### 发送个人介绍（主动消息）

```bash
openclaw message send \
  --channel feishu \
  --target "{user_openid}" \
  --message "$(cat /root/.openclaw/workspace-resume-manager/output/latest_intro.txt)"
```

### 创建定时发送任务

```bash
openclaw cron add \
  --name "每日个人介绍" \
  --cron "0 9 * * *" \
  --tz "Asia/Shanghai" \
  --message "【定时个人介绍】\n\n$(cat /root/.openclaw/workspace-resume-manager/output/latest_intro.txt)" \
  --deliver \
  --channel feishu \
  --to "{user_openid}"
```

### 查询定时任务

```bash
openclaw cron list
```

### 删除定时任务

```bash
openclaw cron remove {jobId}
```

---

## 💬 用户交互模板

### 发送介绍成功

```
🪪 个人介绍已发送！

📅 更新时间: {时间}
📝 内容来源: 岁时记简历数据

介绍包含：
• 基本信息与联系方式
• 核心技能标签
• 工作经历概览
• 精选项目展示
```

### 更新介绍成功

```
✅ 个人介绍已更新！

📊 更新内容：
• 基本信息: {姓名} | {职位}
• 技能标签: {技能数量} 项
• 工作经历: {经历数量} 条
• 精选项目: {项目数量} 个

🪪 已发送最新版本~
```

### 设置定时任务成功

```
⏰ 定时任务已设置！

📅 发送时间: {时间描述}
🔄 频率: {频率}
📝 内容: 自动生成的个人介绍

说"取消定时介绍"可随时停止~
```

### 查询定时任务

```
📋 你的定时任务：

1. 🔄 每日个人介绍 - 每天09:00

说"取消xx"可删除~
```

---

## 🎯 使用场景示例

### 场景1：立即发送介绍

**用户**: 发我的介绍

**AI 执行**:
```bash
cd /root/.openclaw/workspace-resume-manager && ./scripts/generate-intro.sh
openclaw message send \
  --channel feishu \
  --target "{openid}" \
  --message "$(cat /root/.openclaw/workspace-resume-manager/output/latest_intro.txt)"
```

**AI 回复**:
```
🪪 个人介绍已发送！
```

---

### 场景2：更新并发送

**用户**: 更新介绍

**AI 执行**:
```bash
cd /root/.openclaw/workspace-resume-manager && ./scripts/generate-intro.sh
openclaw message send \
  --channel feishu \
  --target "{openid}" \
  --message "$(cat /root/.openclaw/workspace-resume-manager/output/latest_intro.txt)"
```

**AI 回复**:
```
✅ 个人介绍已更新并发送！
```

---

### 场景3：定时发送

**用户**: 每天早上9点发我的介绍

**AI 执行**:
```bash
# 先生成最新介绍
cd /root/.openclaw/workspace-resume-manager && ./scripts/generate-intro.sh

# 创建定时任务
openclaw cron add \
  --name "每日个人介绍" \
  --cron "0 9 * * *" \
  --tz "Asia/Shanghai" \
  --message "🪪 早安！今日个人介绍\n\n$(cat /root/.openclaw/workspace-resume-manager/output/latest_intro.txt)" \
  --deliver \
  --channel feishu \
  --to "{openid}"
```

**AI 回复**:
```
⏰ 收到，每天早上9点自动发送你的个人介绍~
```

---

### 场景4：取消定时

**用户**: 取消定时介绍

**AI 执行**:
1. `openclaw cron list` 查找任务
2. `openclaw cron remove {jobId}` 删除

**AI 回复**:
```
✅ 已取消每日个人介绍定时任务
```

---

## 📁 文件结构

```
workspace-resume-manager/
├── scripts/
│   └── generate-intro.sh      # 介绍生成脚本
├── output/
│   ├── latest_intro.txt       # 最新介绍
│   └── intro_YYYYMMDD_HHMMSS.txt  # 历史版本
└── skills/
    └── feishu-intro.md        # 本技能文档
```

---

## ⚠️ 重要说明

### Feishu 主动消息权限

定时发送功能需要**主动消息权限**，请确保：
1. 机器人已申请并获得了主动消息权限
2. 用户已与机器人有过互动（24小时内）

### 内容更新机制

个人介绍内容来源于：
- `data/profile.json` - 基本信息
- `data/skills.json` - 技能标签
- `data/experience.json` - 工作经历
- `data/projects.json` - 项目展示

更新简历数据后，执行 `更新介绍` 即可同步最新内容。

---

## 🎨 个性化配置

可通过修改 `scripts/generate-intro.sh` 自定义：
- 介绍卡片样式
- 显示的信息字段
- 更新频率
- 消息模板
